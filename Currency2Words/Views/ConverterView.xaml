<Window x:Class="Currency2Words.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Currency2Words"
        xmlns:viewmodel="clr-namespace:Currency2Words.ViewModels"
        mc:Ignorable="d"
        Title="MainWindow" Height="350" Width="525" Background="Black">
    <Window.DataContext>
        <viewmodel:ConverterViewModel/>
    </Window.DataContext>
    <Grid Margin="100 40" >
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="2*"/>
        </Grid.RowDefinitions>
        <TextBox Grid.Row="0" Text="{Binding Input, UpdateSourceTrigger=PropertyChanged}" 
                 HorizontalContentAlignment="Center" 
                 VerticalContentAlignment="Center"
                 FontSize="24"
                 MaxLength="12"/>
        <Button Grid.Row="1" Content="Convert2Words" 
                Command="{Binding ConvertCommand, UpdateSourceTrigger=PropertyChanged}"
                Margin="0 10"/>
        <Label Grid.Row="2" Foreground="Azure" Content="{Binding Words}" HorizontalContentAlignment="Center" VerticalContentAlignment="Center"/>
    </Grid>
</Window>
